#include "stdio.h"

int main()
{

    int a, b, pow, i;

    printf("Digite um valor para a: ");
    scanf("%d", &a);

    printf("Digite um valor para b: ");
    scanf("%d", &b);


    for(i = 1; i < b; i++)
    {
        pow = pow + a;
    }
    
    printf("%d", a);

}